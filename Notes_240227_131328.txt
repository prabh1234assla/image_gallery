Write about any five instructions in each group of the 8085 microprocessor instruction set.

Write an 8085 assembly language program using a minimum number of instructions to add the 16 bit no. in BC, DE and HL. Store the 16-bit result in DE pair.

Explain with examples various Addressing Modes and Instruction Types in 8085 mp.

Indicate the values stored in the stack and the content of the stack pointer after executing the following set of 8051 instructions assuming that the RSO and RS1 bits in the PSW register are 0 and 1.

MOV R0, #25H;

MOV R2, #12H;

MOV R4, #0F3H;

PUSH 2;

PUSH 0;

PUSH 4;